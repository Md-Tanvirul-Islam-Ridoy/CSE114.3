#include <stdio.h> 
int main() 
{ 
    int n; 
    printf("Enter the number:"); 
    scanf("%d",&n); 
    if(n%3==0 && n%8==0){ 
        printf("%d is divisible by 3 and 8.",n); 
    } 
    else{ 
        printf("%d is not divisible by 3 and 8.",n); 
    } 
    return 0; 
}
