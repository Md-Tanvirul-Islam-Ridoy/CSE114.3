#include <stdio.h> 
int main() 
{ 
    int sum=0; 
    printf("The first 20 natural numbers are:"); 
    for(int i=1;i<=20;i++){ 
        sum=sum+i; 
        printf("%d ",i); 
    } 
     printf("\nSum of first 20 natural numbers = %d",sum); 
    return 0; 
} 
